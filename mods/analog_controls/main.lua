-- Analog Controls mod for SM64 DS COOP.
-- The native port reads this mod's presence and exposes the live toggle in F5.
if sm64ds and sm64ds.log then
    sm64ds.log("Analog Controls loaded: use F5 to toggle it")
end